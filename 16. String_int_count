# -------------------------------------------#
# Циклы, строки, списки
# -------------------------------------------#

#  Вывод чисел строки, которые встречаются более одного раза
string = input()
numbers = [int(i) for i in string.split()]

for i in set(numbers):
    count = numbers.count(i)
    if count != 1:
        print(i, end=' ')
